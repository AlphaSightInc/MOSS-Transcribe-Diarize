.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Local model artifacts.
pretrained/
outputs/
examples/*
!examples/*.md
runs/
live-runs/

# Large or transient runtime files.
*.safetensors
*.bin
*.pt
*.pth
*.onnx
*.log

# Live access credentials and operator pairing material.
*.key
*.pem
*live-auth*.json
*pairing-payload*

# Host-local live service profile. The tracked template is ops/moss-live.env.example; the
# filled-in file names host paths and belongs only to the deployment host.
ops/moss-live.env

# Code-signing material for the local macOS capture app. A .p12 carries a private
# key and a .keychain-db carries the signing identity; neither may ever be committed.
*.p12
*.crt
*.keychain-db
*.keychain
