# Campaign A execution contract

**Ralph contract:** `moss-l2-retained-identity-v1`

Authority is Campaign A only, A0 through A6, maximum ten iterations. The controlling
plan is `docs/plans/r1-stage0-minimum-seam-0803.md`, SHA-256
`2bb23820f947d884710cd65f59dbfd95aeaa87957d84b2846751239602ef7407`.

No product code, product tests, deployment, service, manifest, host, TCC, volume,
retained-audio session, push, or keeper merge is authorized. Follow A0 through A6
strictly. Stop at every stage boundary for supervisor review. Any predeclared gate
failure records `BLOCKED` and ends the campaign.

The corpus pin is deliberately `UNFROZEN` through A0. Gated work must refuse until
the A1 manifest and its real SHA-256 pin land in the same commit.
